@model int
@{
	var hours = Model / 60;
	var mins = Model % 60;
	if(hours == 1)
	{
		@:1 time
	}
	else if (hours > 1)
	{
		@:@hours timer
	}

	if(hours > 0 && mins > 0)
	{
		@:og
	}

	if (mins == 1)
	{
		@:1 minutt
	}
	else if (mins > 1)
	{
		@:@mins minutter
	}
}
